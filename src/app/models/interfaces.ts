export interface IProduct {
  id: string;
  name: string;
  description: string;
  price: number;
  units?: number;
}
export interface IMeat {
  id: string;
  name: string;
  description: string;
  price: number;
  units?: number;
}
